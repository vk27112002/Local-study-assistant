# Copy this file to a new file named exactly ".env" (same folder) and paste your real key below.
# .env is git-ignored, so it will never be committed or accidentally shared.

GROQ_API_KEY=gsk_your_real_key_here
# Optional: override the default model without editing code.
GROQ_MODEL=openai/gpt-oss-120b
